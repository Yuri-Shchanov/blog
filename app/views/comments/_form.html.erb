<% @url = "users/" + current_user[:id].to_s + "/articles/" + @article.id.to_s + "/comments" %>
<%= form_for(@comment, :url => @url) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>